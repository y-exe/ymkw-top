---
import Layout from '../layouts/Layout.astro';
import { History, ArrowLeft } from 'lucide-react';
---

<Layout title="No Data Yet">
    <div class="h-[70vh] flex flex-col items-center justify-center animate-fade-in p-6">
        <div class="bg-white p-10 rounded-[2.5rem] border border-gray-200 shadow-xl max-w-md w-full text-center relative overflow-hidden">
            {}
            <div class="absolute top-0 left-0 w-full h-2 bg-gray-200"></div>
            
            <div class="w-20 h-20 bg-gray-50 text-gray-400 rounded-3xl flex items-center justify-center mx-auto mb-6">
                <History className="w-10 h-10" />
            </div>

            <h2 class="text-2xl font-black text-gray-900 mb-3 font-outfit">Not Yet Generated</h2>
            <p class="text-sm text-gray-500 mb-8 leading-relaxed">
                全期間の集計データ（スナップショット）は<br/>
                まだ作成されていません。<br/>
                <span class="text-xs text-gray-400 mt-2 block">管理者が Discord で /open コマンドを実行すると<br/>ここに表示されます。</span>
            </p>

            <button onclick="history.back()" class="w-full py-4 bg-gray-900 text-white font-bold rounded-2xl hover:bg-black transition-all active:scale-[0.98] shadow-lg flex items-center justify-center gap-2">
                <ArrowLeft className="w-4 h-4" />
                戻る
            </button>
        </div>
    </div>
</Layout>