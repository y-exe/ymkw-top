---
import Layout from '../layouts/Layout.astro';
import { AlertTriangle, RotateCcw, Terminal } from 'lucide-react';
---

<Layout title="404 Not Found">
    {}
    <div class="h-full flex flex-col items-center justify-center animate-fade-in p-4 min-h-[80vh]">
        
        <div 
            class="w-full bg-white border border-gray-200 rounded-3xl shadow-xl shadow-gray-200/50 overflow-hidden relative mx-auto"
            style="max-width: 400px;"
        >
            
            {}
            <div class="bg-red-50 border-b border-red-100 px-6 py-4 flex items-center gap-3">
                <div class="p-2 bg-white rounded-full border border-red-100 shadow-sm flex-shrink-0">
                    <AlertTriangle className="w-5 h-5 text-[#ff0000]" />
                </div>
                <div>
                    <p class="text-xs font-bold text-red-500 uppercase tracking-wider leading-tight">System Alert</p>
                    <p class="text-sm font-bold text-gray-900 leading-tight">Page Not Found</p>
                </div>
            </div>

            {}
            <div class="p-8 text-center">
                
                {}
                <h1 class="text-8xl font-black text-gray-900 mb-2 select-none tracking-tighter font-outfit leading-none">
                    404
                </h1>
                
                <p class="text-gray-500 font-medium text-sm mb-8">
                    このページは存在しないか、<br/>
                    すでに削除されています。
                </p>

                {}
                <div class="bg-gray-900 rounded-xl p-4 text-left mb-8 border border-gray-800 shadow-inner group">
                    <div class="flex items-center gap-1.5 mb-3 border-b border-gray-800 pb-2">
                        <Terminal className="w-3 h-3 text-gray-500" />
                        <span class="text-[10px] text-gray-400 font-mono">System Log</span>
                    </div>
                    <div class="font-mono text-xs space-y-1.5">
                        <p class="text-red-400 font-bold flex justify-between">
                            <span>Error: NOT_FOUND</span>
                            <span class="text-[10px] text-gray-600">404</span>
                        </p>
                        <p class="text-gray-500 truncate">Path: <span id="path-display" class="text-gray-300">...</span></p>
                        <p class="text-gray-500">Status: <span class="text-yellow-500">Unreachable</span></p>
                    </div>
                </div>

                {}
                <div class="flex flex-col">
                    <button 
                        onclick="history.back()" 
                        class="w-full flex items-center justify-center gap-2 px-4 py-3.5 bg-white text-gray-900 border border-gray-200 rounded-xl font-bold text-sm hover:bg-gray-50 hover:border-gray-300 transition-all shadow-sm active:scale-[0.98]"
                    >
                        <RotateCcw className="w-4 h-4 text-gray-500" />
                        <span>前のページに戻る</span>
                    </button>
                </div>
            </div>

            {}
            <div class="h-1.5 w-full bg-[repeating-linear-gradient(45deg,#f9fafb,#f9fafb_10px,#f3f4f6_10px,#f3f4f6_20px)]"></div>
        </div>

    </div>
</Layout>

<script>
    const pathSpan = document.getElementById('path-display');
    if (pathSpan) {
        const path = window.location.pathname;
        pathSpan.textContent = path.length > 25 ? path.substring(0, 25) + '...' : path;
    }
</script>